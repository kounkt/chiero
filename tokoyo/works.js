/* works.js — 数式スケッチの正本。ここが唯一の定義場所。
   ブラウザ（index.html）と検査（tools/verify.mjs）が同じこのファイルを読む。
   座標系は 0..400。kami() が実行中の関数の原文をそのまま作品の中へ描く。

   様式（2026-08-01 本人判断で白紙様式から変更）:
   地は墨 #0B0B0D、点は光 #FFFFFF、落款は赤 #E60012 の一点。
   全作品が残像を持つ（trail）。残像は「時間が画に溜まる」ことで層と奥行きを生む。 */

const STYLE = { dark: true };

/* 方眼（grid）の掟:
   「そこに居るもの」＝生き物には敷かない。周りが測れない方が浮く。
   「場・構造そのもの」＝流れ・波・幾何には敷く。場を描いているのだから座標系があるのが正しい。 */

const WORKS = [

  // 幾何 — 64本の大円を黄金角で傾けて球を編む。息をするように半径が揺れる
  { slug: '006_mayu', name: '繭', grid: true, ink: 0.22, trail: 0.84, n: 40000, loop: 2,
    f: (i, t,
        j = i % 64, p = (i / 64 | 0) / 625,
        a = p * TAU, c = (j * .618034 % 1) * PI, h = j * 2.39996 + t * .5,
        R = 132 + 7 * sin(t * 2 + j),
        x = cos(a), y = sin(a) * cos(c), z = sin(a) * sin(c),
        Q = [200 + R * (x * cos(h) + z * sin(h)),
               200 + R * y * .98 + R * (z * cos(h) - x * sin(h)) * .12],
        Dx = 200 + 73 * sin(t + 0.92) + 22 * sin(t * 2 + 1.56),
        Dy = 200 + 42 * cos(t + 1.2) + 16 * sin(t * 3 + 0.92)
       ) => [Dx + (Q[0] - 200) * .68, Dy + (Q[1] - 200) * .68] },

  // 波 — 円い水面。三つの波源へ向かう向きに点を寄せ、重なりに明暗の環が立つ
  { slug: '005_sazanami', name: '漣', grid: true, ink: 0.22, trail: 0.82, n: 40000, loop: 1,
    f: (i, t,
        a = i * 2.39996, d = sqrt(i / 40000), r = 168 * d,
        x = r * cos(a), y = r * sin(a),
        p = hypot(x + 62, y + 30) + 22,
        q = hypot(x - 72, y - 46) + 22,
        u = 9 * cos(p * .22 - t * 2) / p,
        v = 8 * cos(q * .19 - t * 2) / q
       ) => [200 + x + u * (x + 62) + v * (x - 72),
             200 + y + u * (y + 30) + v * (y - 46)] },

  // 象り — どこから放しても同じ環へ落ちる。落ちる速さは近づくほど緩む
  { slug: '015_tani', name: '谷', grid: true, ink: 0.20, trail: 0.88, n: 40000, loop: 2,
    f: (i, t,
        j = i % 120, k = (i / 120 | 0) / 333,
        w = (k * .30 + t / (TAU * 2) + (j * .618034 % 1)) % 1,
        R0 = 30 + 168 * (j * .754878 % 1),
        V = 92 + 4 * sin(t),
        r = V + (R0 - V) * exp(-4.6 * w) + 6 * sin(w * TAU * 3 + j),
        a = j * 2.39996 + w * 1.35 + t * .5
       ) => [200 + r * cos(a), 200 + r * sin(a)] },

  // 場 — 太陽が進み、惑星はその通り道に螺旋を残す。軌道周期を整数比に丸めて像を閉じた
  { slug: '014_kouseki', name: '航跡', grid: true, ink: .46, trail: .74, n: 40000, loop: 1,
    f: (i, t,
        b = i < 2600, a = i < 5200, q = (i - 2600) / 2600,
        j = i - 5200, k = j % 8, p = (j / 8 | 0) / 4350,
        m = [1, 2, 3, 4, 6, 8, 12, 24][k],
        w = 24 / m, R = 11 * m ** .667,
        u = p ** .85, c = k * .8 + w * (t - 2 * u),
        d = i * 2.39996, e = sqrt(i / 2600)
       ) => b
      ? [330 + 17 * e * cos(d), 206 + 17 * e * sin(d)]
      : a
      ? (i % 5) / 5 < q * .92 ? [-9, -9]
        : [330 - 310 * q, 206 - 54 * q + 1.4 * (i * .754878 % 1 - .5)]
      : (i % 7) / 7 < u * .86 ? [-9, -9]
        : [330 - 300 * u + R * sin(c) * .3, 206 - 54 * u + R * cos(c) * .95] },

  // 動物 — 8本の腕が巻いてはほどける。位相をずらしたので順に波が回る
  { slug: '010_tako', name: '蛸', ink: 0.3, trail: 0.88, n: 40000, loop: 1,
    f: (i, t,
        b = i < 12000, j = i - 12000, k = j % 8, u = (j / 8 | 0) / 3500,
        g = i * 2.39996, d = sqrt(i / 12000),
        A = k / 8 * TAU + .2,
        c = A + 2.1 * u ** 1.5 * (.55 + .5 * sin(t * 2 + k * .9)) + .22 * sin(u * 4 - t * 3 + k),
        r = 40 + 132 * u * (1 - .28 * u),
        W = 11 * (1 - u) ** .7 * ((j * .754878 % 1) * 2 - 1),
        Q = b
        ? [200 + 70 * sin(d * 1.5708) * cos(g),
           138 + 70 * sin(d * 1.5708) * sin(g) * .82 + 40 * d * d + 7 * sin(t * 2)]
        : [200 + r * cos(c + W / r), 176 + r * sin(c + W / r) * .74 + 28 * u],
        Dx = 200 + 73 * sin(t + 4.23) + 22 * sin(t * 2 + 0.91),
        Dy = 200 + 42 * cos(t + 5.5) + 16 * sin(t * 3 + 4.23)
       ) => [Dx + (Q[0] - 200) * .68, Dy + (Q[1] - 200) * .68] },

  // 場 — 何かの生き物ではない。半径ごとに違う速さで回る点の場。渦の腕は数式が作る
  { slug: '012_uzu', name: '渦', grid: true, ink: .34, trail: .88, n: 40000, loop: 4,
    f: (i, t,
        g = i * .618034 % 1, h = i * .754878 % 1, q = i * .414214 % 1,
        d = 26 + 148 * sqrt(g),
        w = (1 + (g * 12 | 0)) * .25,
        a = h * TAU + .62 * sin(h * TAU * 3 - 3.4 * log(d / 26)) + t * w,
        r = d + 13 * sin(d * .12 - t * 2 + q * 6)
       ) => [200 + r * cos(a), 200 + r * sin(a) * .8 + 14 * sin(a * 2 - t)] },

  // 動物 — 傘が中心から縁へ収縮波を送り、触手がそれに遅れて追う
  { slug: '001_kurage', name: '水母', ink: 0.3, trail: 0.88, n: 40000, loop: 1,
    f: (i, t,
        b = i * 2.39996, d = sqrt(i / 25600),
        r = 110 * sin(d * 1.5708) * (1 + .13 * sin(d * 6 - t * 3)),
        j = i - 25600, m = j % 20, s = (j / 20 | 0) / 720,
        q = 58 * (1 - .24 * s), a = m / 20 * TAU,
        Q = i < 25600
        ? [200 + r * cos(b), 138 + r * sin(b) * .36 + 52 * d * d + 8 * sin(t * 3)]
        : [200 + q * cos(a) + 19 * s * sin(s * 5 - t * 3 + m) + 7 * s * sin(t * 2 + m * 3),
           178 + q * sin(a) * .36 + 172 * s + 8 * sin(s * 9 - t * 3 + m)],
        Dx = 200 + 46 * sin(t + 0.0) + 22 * sin(t * 2 + 0.0),
        Dy = 200 + 50 * cos(t + 0.0) + 16 * sin(t * 3 + 0.0)
       ) => [Dx + (Q[0] - 200) * .68, Dy + (Q[1] - 200) * .68] },

  // 花 — 44枚の花弁を黄金角で並べ、開く波を中心から外へ送る。真上から見た薔薇
  { slug: '002_hana', name: '花', ink: 0.26, trail: 0.84, n: 40000, loop: 2,
    f: (i, t,
        j = i % 48, p = (i / 48 | 0),
        u = p * .618034 % 1, v = (p * .754878 % 1) * 2 - 1,
        g = j / 48, h = j * 2.39996 + t * .5,
        B = 6 + 30 * g, L = 30 + 84 * g,
        R = B + L * u,
        W = L * .55 * sin(PI * u ** .8) ** .7,
        A = 1.3 - 2.1 * g + .28 * sin(t * 2 - g * 5),
        x = R * cos(h) - v * W * sin(h),
        z = R * sin(h) + v * W * cos(h),
        y = L * (A * u - .5 * u ** 3) + .4 * W * v * v,
        Q = [200 + x * .88, 198 + z * .76 - y * .4],
        Dx = 200 + 55 * sin(t + 2.4) + 22 * sin(t * 2 + 4.08),
        Dy = 200 + 58 * cos(t + 3.12) + 16 * sin(t * 3 + 2.4)
       ) => [Dx + (Q[0] - 200) * .68, Dy + (Q[1] - 200) * .68] },

  // 幾何 — 対数螺旋に沿って管を掃く。殻の厚みを波が外へ伝わる
  { slug: '003_kai', name: '貝', grid: true, ink: 0.24, trail: 0.82, n: 40000, loop: 1,
    f: (i, t,
        j = i % 160, p = (i / 160 | 0) / 250,
        h = p * TAU * 3.1,
        R = 150 * exp((h - 19.5) * (.285 + .028 * sin(t))),
        r = R * .42 * (1 + .13 * sin(t * 2 - h * 3)),
        c = j / 160 * TAU, k = R + r * cos(c),
        Q = [152 + k * cos(h) * .92,
               196 + k * sin(h) * .88 - r * sin(c) * .3],
        Dx = 200 + 64 * sin(t + 4.8) + 22 * sin(t * 2 + 1.88),
        Dy = 200 + 34 * cos(t + 6.24) + 16 * sin(t * 3 + 4.8)
       ) => [Dx + (Q[0] - 200) * .68, Dy + (Q[1] - 200) * .68] },

  // 流れ — 円柱をよける流線。ψ = U y (1 − a²/r²) を r について解いた形
  { slug: '004_ushio', name: '潮', grid: true, ink: 0.3, trail: 0.92, n: 40000, loop: 2,
    f: (i, t,
        j = i / 4 | 0, c = i % 4,
        m = j % 34, e = m % 2 ? 1 : -1,
        C = 8 + 15 * (m / 2 | 0),
        s = ((j / 34 | 0) / 294 + t / TAU * 1.5 + c * .0012) % 1,
        p = PI * (1 - s), q = sin(p),
        r = (C + sqrt(C * C + 14400 * q * q)) / (2 * q),
        x = r * cos(p) * .62, y = e * r * q * .62
       ) => (i % 9) / 9 < max((abs(x) - 108) / 86, (abs(y) - 96) / 58) ? [-9, -9]
                    : [200 + x, 200 + y] },

  // 場 — 深海を漂う帯。ねじれを進行波で送り、面が裏返るところに光が集まる
  { slug: '013_obi', name: '帯', grid: true, ink: .28, trail: .9, n: 40000, loop: 1,
    f: (i, t,
        u = i * .618034 % 1, v = (i * .754878 % 1) * 2 - 1,
        z = 1.25 * sin(u * 4 - t * 2) + .6 * sin(u * 7 + t),
        w = 64 * sin(PI * u ** .8) ** .5,
        y = 200 + 64 * sin(u * 3 - t * 2) * (.3 + u * .7),
        Q = [26 + 348 * u, y + v * w * cos(z) + v * w * sin(z) * .3],
        Dx = 200 + 55 * sin(t + 2.75) + 22 * sin(t * 2 + 4.67),
        Dy = 200 + 58 * cos(t + 3.58) + 16 * sin(t * 3 + 2.75)
       ) => [Dx + (Q[0] - 200) * .68, Dy + (Q[1] - 200) * .68] },

  // 動物 — うねる軟体と、位相をずらして揺れる60本の突起。生き物感は縁の毛から立つ
  { slug: '007_umiushi', name: '海牛', ink: 0.28, trail: 0.88, n: 40000, loop: 1,
    f: (i, t,
        b = i < 17000, j = i - 17000, k = j % 80, e = k % 2 ? 1 : -1,
        u = (j / 80 | 0) / 288,
        s = b ? i * .618034 % 1 : .05 + .9 * ((k / 2 | 0) / 40),
        c = (i * .754878 % 1) * PI,
        p = -1.45 + 2.9 * s + .5 * sin(t - s * 3),
        g = 2.9 - 1.5 * cos(t - s * 3),
        cx = 200 + 126 * sin(p), cy = 238 - 120 * cos(p) + 22 * sin(s * 2 - t),
        qx = 126 * cos(p) * g, qy = 120 * sin(p) * g + 44 * cos(s * 2 - t),
        m = hypot(qx, qy), nx = -qy / m, ny = qx / m, hx = qx / m, hy = qy / m,
        W = 39 * sin(PI * s ** .7) ** .55,
        w = .55 * sin(t * 2 - s * 4 + k * .3),
        dx = nx * cos(w) - ny * sin(w), dy = nx * sin(w) + ny * cos(w),
        L = 70 * u * (1 - .28 * u) * sin(PI * s ** .8) ** .35,
        Q = b
        ? [cx + W * cos(c) * nx, cy + W * cos(c) * ny - W * sin(c) * .38]
        : [cx + e * (W * .8 + L) * dx - 20 * u * u * hx,
           cy + e * (W * .8 + L) * dy - 20 * u * u * hy],
        Dx = 200 + 46 * sin(t + 3.32) + 22 * sin(t * 2 + 5.64),
        Dy = 200 + 50 * cos(t + 4.32) + 16 * sin(t * 3 + 3.32)
       ) => [Dx + (Q[0] - 200) * .68, Dy + (Q[1] - 200) * .68] },

  { slug: '008_umishida', name: '海羊歯', ink: 0.28, trail: 0.88, n: 40000, loop: 1,
    f: (i, t,
        k = i % 10, j = i / 10 | 0, h = j < 800,
        n = j - 800, q = n % 40, e = (n / 40 | 0) % 2 ? 1 : -1, w = (n / 80 | 0) / 40,
        a = h ? j / 800 : .12 + .8 * (q / 40),
        A = k / 10 * TAU + .38 * sin(a * 3 - t * 2 + k),
        r = 26 + 142 * a,
        z = 46 * sin(a * 2.1) - 66 * a * a + 8 * sin(t * 2 - a * 4 + k),
        x = 200 + r * cos(A), y = 196 + r * sin(A) * .78 - z * 1.05,
        L = 34 * sin(PI * a) ** .6 * w * (1 - .3 * w),
        c = A + e * (1.15 + .3 * sin(w * 3 - t * 2 + q * .5)),
        Q = h ? [x + 1.4 * (j * .618 % 1 - .5), y + 1.4 * (j * .754 % 1 - .5)]
                : [x + L * cos(c), y + L * sin(c) * .8],
        Dx = 200 + 55 * sin(t + 5.72) + 22 * sin(t * 2 + 3.44),
        Dy = 200 + 58 * cos(t + 1.15) + 16 * sin(t * 3 + 5.72)
       ) => [Dx + (Q[0] - 200) * .68, Dy + (Q[1] - 200) * .68] },

  // 動物 — 5尾が別々の速さで泳ぐ。体はしなり、尾は薄い帯として遅れて揺れる
  { slug: '009_kingyo', name: '金魚', ink: 0.28, trail: 0.92, n: 40000, loop: 2,
    f: (i, t,
        m = i % 5, j = i / 5 | 0, b = j < 4600,
        s = b ? j * .618034 % 1 : 1 + .5 * ((j - 4600) * .618034 % 1),
        v = (j * .754878 % 1) * 2 - 1,
        L = 55 + 9 * m, p = m * 1.9, w = [1, .5, 1.5, 2, .5][m],
        A = t * w + p, R = 52 + 15 * m,
        cx = 200 + R * cos(A), cy = 196 + R * sin(A) * .46 + 16 * (m - 2),
        ax = -sin(A), ay = cos(A) * .46, q = hypot(ax, ay),
        H = b ? L * .38 * sin(PI * s ** .7) ** .58
              : L * (.05 + .44 * (s - 1) / .5) * (1 + .22 * sin(s * 9 - t * 5 + v * 3)),
        Y = L * .12 * s ** 1.8 * sin(s * 2.6 - t * 4 + p) + v * H,
        X = L * (s - .45),
        Q = [cx + (X * ax - Y * ay) / q, cy + (X * ay + Y * ax) / q],
        Dx = 200 + 64 * sin(t + 1.83) + 22 * sin(t * 2 + 3.11),
        Dy = 200 + 34 * cos(t + 2.38) + 16 * sin(t * 3 + 1.83)
       ) => [Dx + (Q[0] - 200) * .68, Dy + (Q[1] - 200) * .68] },

  // 動物 — 前翅と後翅が位相をずらして羽ばたく。翅は反り、根元の重なりが体になる
  { slug: '011_chou', name: '蝶', ink: 0.26, trail: 0.86, n: 40000, loop: 1,
    f: (i, t,
        e = i % 2 ? 1 : -1, j = i / 2 | 0,
        b = j < 10000, d = j >= 18600, n = b ? j : j - 10000,
        u = n * .618034 % 1, v = sin((n * .754878 % 1 - .5) * PI),
        S = b ? 148 : 104, C = b ? 62 : 50,
        h = .92 * sin(t * 2 + (b ? 0 : .34)),
        W = C * sin(PI * u ** (b ? .6 : .8)) ** (b ? .8 : .6),
        X = e * S * u * cos(h) * (1 - .08 * v * v),
        Z = S * u * sin(h),
        Y = (b ? -16 : 46) + W * v + (b ? 28 : 18) * u ** 1.4,
        m = j - 18600, g = m / 1400, a = (g - .5) * 2,
        Q = d
        ? g < .5
          ? [200 + e * 5.5 * sin(PI * (g * 2) ** .5) * (m * .754878 % 1 * 2 - 1),
             168 + 150 * g * 2]
          : [200 + e * (5 + 30 * (g * 2 - 1) ** .8),
             170 - 40 * (g * 2 - 1) + 13 * (g * 2 - 1) ** 2]
        : [200 + X, 190 + Y - Z * .5],
        Dx = 200 + 46 * sin(t + 0.35) + 22 * sin(t * 2 + 0.59),
        Dy = 200 + 50 * cos(t + 0.45) + 16 * sin(t * 3 + 0.35)
       ) => [Dx + (Q[0] - 200) * .68, Dy + (Q[1] - 200) * .68] },

  /* ここから先は「象り」。人のなかで起きていることと同じ形をしたもの。
     効き目を約束する式は書かない。起きていることの形を、外から見せるだけ。
     場を描いているので漂流させず、方眼を敷く。 */


  // 象り — 二つの群れが別々の場所から放たれ、逆向きに巻いて一つの環に同居する。目盛りが違うので重なりに縞が出る
  { slug: '016_yose', name: '寄せ', grid: true, ink: 0.19, trail: 0.88, n: 40000, loop: 2,
    f: (i, t,
        e = i % 2 ? 1 : -1, j = (i / 2 | 0) % 96, k = (i / 192 | 0) / 208,
        w = (k * .92 + t / (TAU * 2) + (j * .618034 % 1)) % 1,
        a0 = j * 2.39996, A = 27 + 9 * sin(j * 2.1),
        Px = 200 + e * 126 + A * cos(a0), Py = 200 - e * 56 + A * sin(a0),
        m = 1 - exp(-5.4 * w),
        r = 88 + 6 * sin(t * 1.5 + j),
        b = a0 * (e > 0 ? 1 : 1.055) + e * w * 1.2 + t * .5,
        Vx = 200 + r * cos(b), Vy = 200 + r * sin(b)
       ) => [Px + (Vx - Px) * m, Py + (Vy - Py) * m] },

  // 象り — 十一枚を一筆で巡る閉じた道。点は道の上にしかいない。握られるところ（中心と先）で帯が締まる
  { slug: '017_meguri', name: 'めぐり', grid: true, ink: 0.18, trail: 0.90, n: 40000, loop: 2,
    f: (i, t,
        j = i % 88, k = (i / 88 | 0) / 455,
        u = (k * .12 + t / (TAU * 2) + (j * .618034 % 1)) % 1,
        h = u * PI, R = 168 * cos(11 * h) * (.84 + .16 * sin(2 * h + t)),
        d = abs(R) / 168,
        W = 13 * sin(PI * d) ** .7 * ((i * .754878 % 1) * 2 - 1),
        g = h, c = g + PI / 2
       ) => [200 + R * cos(g) + W * cos(c), 200 + R * sin(g) + W * sin(c)] },

  // 象り — 八十一の世界。内へ行くほど細かい違いが先に消え、粗い違いが後から消える。中心は空けてある
  { slug: '018_kaname', name: 'かなめ', grid: true, ink: 0.21, trail: 0.86, n: 40000, loop: 2,
    f: (i, t,
        j = i % 81, u = (i / 81 | 0) / 494,
        d0 = j % 3 - 1, d1 = (j / 3 | 0) % 3 - 1,
        d2 = (j / 9 | 0) % 3 - 1, d3 = (j / 27 | 0) % 3 - 1,
        T = t * .5,
        w0 = 1 / (1 + exp((u - .22 - .05 * sin(T)) * 34)),
        w1 = 1 / (1 + exp((u - .44 - .05 * sin(T + 2)) * 34)),
        w2 = 1 / (1 + exp((u - .66 - .05 * sin(T + 4)) * 34)),
        w3 = 1 / (1 + exp((u - .96 - .05 * sin(T + 6)) * 34)),
        h = d3 * (TAU / 3) * w3 + d2 * (TAU / 9) * w2
          + d1 * (TAU / 27) * w1 + d0 * (TAU / 81) * w0 + T,
        r = 46 + 140 * (1 - u)
       ) => [200 + r * cos(h), 200 + r * sin(h)] },

  // 象り — 二つの居場所が半回転で入れ替わる。ひと巡りの終わりに着く先が、始まりに立っていた場所
  { slug: '019_saki', name: 'さきの谷', grid: true, ink: 0.20, trail: 0.88, n: 40000, loop: 2,
    f: (i, t,
        j = i % 150, k = (i / 150 | 0) / 267,
        T = (t / (TAU * 2)) % 1, y = PI * T,
        Ax = 200 + 110 * cos(y), Ay = 200 + 110 * sin(y),
        Bx = 400 - Ax, By = 400 - Ay,
        b = .08 + .78 * (j * .618034 % 1),
        z = min(1, max(0, (T - b) / .06)), m = z * z * (3 - 2 * z),
        a = k * TAU + j * 2.39996 + t * 1.5,
        rr = (40 + 22 * (k * 7 * .754878 % 1)) * (1 - .62 * 4 * m * (1 - m))
       ) => [Ax + (Bx - Ax) * m + rr * cos(a),
             Ay + (By - Ay) * m + rr * sin(a)] },

  /* ここから先は「境」。こちら側で「ここから違う」と決めた線を、向こう側で探したもの。
     線は引かれたものではなく、**同じ高さのところを繋ぐと、ひとりでにそうなる**。 */

  // 境 — 幹から三方へ四代くだる。枝先は81。育ちきると消えて、また落ちる
  { slug: '020_ikazuchi', name: '雷', grid: true, ink: 0.30, trail: 0.93, n: 40000, loop: 2,
    f: (i, t,
        j = i % 81, u = (i / 81 | 0) / 494,
        g = (t / (TAU * 2)) % 1,
        d0 = j % 3 - 1, d1 = (j / 3 | 0) % 3 - 1,
        d2 = (j / 9 | 0) % 3 - 1, d3 = (j / 27 | 0) % 3 - 1,
        a0 = 1.5708 + .07 * d0 + .19 * sin(j * 2.4),
        a1 = a0 + .14 * d1 + .21 * sin(j * 1.7),
        a2 = a1 + .62 * d2 + .23 * sin(j * 3.1),
        a3 = a2 + .92 * d3 + .25 * sin(j * 5.3),
        L0 = 118, L1 = 96,
        L2 = 74 * (1 - .54 * abs(d2)),
        L3 = 58 * (1 - .58 * abs(d3)) * (1 - .34 * abs(d2)),
        s0 = min(1, max(0, u * 4)), s1 = min(1, max(0, u * 4 - 1)),
        s2 = min(1, max(0, u * 4 - 2)), s3 = min(1, max(0, u * 4 - 3)),
        X = 200 + L0 * cos(a0) * s0 + L1 * cos(a1) * s1 + L2 * cos(a2) * s2 + L3 * cos(a3) * s3,
        Y = 24 + L0 * sin(a0) * s0 + L1 * sin(a1) * s1 + L2 * sin(a2) * s2 + L3 * sin(a3) * s3
       ) => u > g ? [-9, -9] : [X, Y] },

  // 境 — 十五基が奥へ小さくなる。柱は内へ転び、笠木は端が反る
  { slug: '021_torii', name: '鳥居', ink: 0.28, trail: 0.86, n: 40000, loop: 2,
    f: (i, t,
        g = i % 15, m = (i / 15 | 0), c = m % 4, v = (m / 4 | 0) / 666,
        p = g / 14, S = pow(.87, g),
        cx = 200 + 46 * sin(p * 2.6 + t * .5) * (1 - p * .6),
        cy = 310 - 214 * pow(p, .78),
        W = 76 * S, H = 128 * S,
        X = c < 2 ? (c ? 1 : -1) * W * (1 - .07 * v)
          : c === 2 ? (v * 2 - 1) * W * 1.24
          : (v * 2 - 1) * W * .90,
        Y = c < 2 ? -H * v
          : c === 2 ? -H - 8 * S - 8 * S * pow(abs(v * 2 - 1), 1.7)
          : -H * .70
       ) => [cx + X, cy + Y] },

  // 境 — 扇。畳まれるほど襞が立ち、開くほど寝る。畳の数は変わらない
  { slug: '022_ori', name: '折り', ink: 0.24, trail: 0.84, n: 40000, loop: 1,
    f: (i, t,
        k = i % 24, m = (i / 24 | 0), u = (m % 60) / 59, v = (m / 60 | 0) / 26,
        O = 1.62 + .96 * sin(t),
        a = ((k + u) / 24 - .5) * O - 1.5708,
        B = sqrt(max(0, 1 - (O / 2.58) ** 2)),
        z = (k % 2 ? 1 : -1) * (u - .5) * 17 * B,
        r = 60 + 120 * v + z * .8
       ) => [200 + r * cos(a), 302 + r * sin(a)] },

  // 境 — 高さの同じところを繋ぐと岸になる。水位が上がると島は分かれ、下がると繋がる
  { slug: '023_shima', name: '島', grid: true, ink: 0.34, trail: 0.80, n: 40000, loop: 1,
    f: (i, t,
        gx = i % 200, gy = (i / 200 | 0),
        x = 3 + gx * 1.97, y = 3 + gy * 1.97,
        H = .52 * sin(x * .0295 + 1.1) * cos(y * .0261 - .4)
          + .27 * sin(x * .0712 - .8) * cos(y * .0634 + 1.7)
          + .14 * sin(x * .151 + 2.3) * cos(y * .129 - 1.1)
          + .07 * sin(x * .317 - .6) * cos(y * .283 + .5),
        d = H - (.04 + .13 * sin(t))
       ) => abs(d) < .019 ? [x, y]
          : d > 0 && (gx * .618034 + gy * .754878) % 1 < .17 ? [x, y] : [-9, -9] },

  // 境 — 三つの石までの近さが等しいところを繋ぐ。石から遠いと筋はまっすぐになる
  { slug: '024_samon', name: '砂紋', grid: true, ink: 0.32, trail: 0.80, n: 40000, loop: 1,
    f: (i, t,
        gx = i % 200, gy = (i / 200 | 0),
        x = 3 + gx * 1.97, y = 3 + gy * 1.97,
        e = min(hypot(x - 128, y - 158) - 17,
            min(hypot(x - 250, y - 116) - 11,
            min(hypot(x - 196, y - 272) - 22, (y - 40) * .92 + 58))),
        q = (e + 5 * sin(t)) / 15
       ) => e < 0 ? (i % 5 ? [-9, -9] : [x, y])
          : (q - floor(q)) < .17 ? [x, y] : [-9, -9] },

  // 海 — 尾は一点から開かず、巻いていく軸に沿って生える。左右交互に六十四本。
  //      軸が遠くへ行くほど余計に巻き、二つの速さの違う波が唸って房になる
  { slug: '025_uo', name: '魚', ink: 0.24, trail: 0.76, n: 40000, loop: 1,
    f: (i, t,
        b = i < 10000, k = i - 10000,
        j = k % 64, m = k / 64 | 0,
        u = (m % 117) / 116, e2 = ((m / 117 | 0) % 4) / 3 * 2 - 1,
        e = j % 2 ? 1 : -1, o = (j / 2 | 0) / 31,
        p = i / 10000,
        c = (i * .754878 % 1) * PI,
        yb = 200 + 24 * sin(p * 3.2 - t * 2) * (.14 + .86 * p * p),
        Wb = 31 * sin(PI * p ** .62) ** .74,
        ry = 200 + 24 * sin(3.2 - t * 2),
        h = .56 * sin(3.2 - t * 2 - 1.15),
        T = h + 1.62 * o ** 1.35,
        R = 138 * o ** .92,
        Sx = 206 + R * cos(T), Sy = ry + R * sin(T),
        q = sin(o * 46 - t * 2) * sin(o * 53 + t),
        n2 = .38 + .62 * q,
        Lb = 92 * sin(PI * o ** .68) ** .55 * n2,
        A = T + e * 1.5708 * (.78 - .30 * o) + e * .62 * u ** 1.25 + .30 * (1 - o) * sin(u * 3 - t * 2),
        rb = Lb * u ** .88,
        W = 3.8 * (1 - u) ** .5 * e2 * abs(e2),
        Q = b
        ? [30 + 166 * p + 4 * sin(c), yb + Wb * cos(c) * (.60 + .40 * sin(p * 6 - t * 2))]
        : [Sx + rb * cos(A) - W * sin(A), Sy + rb * sin(A) + W * cos(A)],
        Dx = 200 + 46 * sin(t + 5.1) + 22 * sin(t * 2 + 2.7),
        Dy = 200 + 50 * cos(t + 1.9) + 16 * sin(t * 3 + 5.1)
       ) => [Dx + (Q[0] - 200) * .68, Dy + (Q[1] - 200) * .68] },

  /* 海 — 翼を前から後ろへ波が渡る。翼端ほど遅れて、大きく振れる。
          ひと打ちは6.7秒、漂流は20秒でひと巡り。**遅さがこの一体の本体**なので、
          漂流の係数は標準の 1・2・3 でなく 0.25・0.5・0.75 に置いている。
          標準のままだと5秒でひと巡りして、実物の四倍で泳ぐ（七世代かけて分かったこと）。 */
  { slug: '026_ei', name: '鱏', ink: 0.24, trail: 0.88, n: 40000, loop: 4,
    f: (i, t,
        b = i < 34000,
        c = i % 200, m = (i / 200 | 0),
        v = b ? (c / 199) * 2 - 1 : 0,
        p = b ? (m % 170) / 169 : (i - 34000) / 6000,
        av = abs(v),
        W = 158 * max(0, sin(PI * p ** .82)) ** .62,
        z = (16 + 30 * av ** 1.9) * sin(p * 2.4 - t * .75 + av * 1.15),
        Q = b
        ? [200 + v * W * (1 - .10 * av * av), 178 + 108 * (p - .5) - z * .74 + 18 * av * av]
        : [200 + 5 * sin(p * 5 - t * .75), 232 + 152 * p ** .9
           + 9 * sin(p * 4 - t * .75 - 1.1) * p],
        Dx = 200 + 34 * sin(t * .25 + 1.4) + 14 * sin(t * .5 + 3.9),
        Dy = 200 + 30 * cos(t * .25 + 2.6) + 11 * sin(t * .75 + 1.4)
       ) => [Dx + (Q[0] - 200) * .74, Dy + (Q[1] - 200) * .74] },

  /* 海 — 外套は硬い。鰭を波が伝い、十本の腕がそれに遅れて従う。
          遅れの量を腕ごとに変えてあるので、束ねて動かない。
          鰭のひと巡りは5秒。漂流は鱏と同じ20秒（標準の1・2・3にすると5秒で、実物の四倍になる） */
  { slug: '027_ika', name: '烏賊', ink: 0.26, trail: 0.88, n: 40000, loop: 4,
    f: (i, t,
        b = i < 13000, h = i >= 13000 && i < 25000,
        c = (i * .754878 % 1) * PI, d = sqrt(i / 13000),
        j = (i - 13000) % 2 ? 1 : -1, q = ((i - 13000) / 2 | 0) / 5999,
        k = (i - 25000) % 10, s = ((i - 25000) / 10 | 0) / 1499,
        wv = sin(q * 3.6 - t),
        Q = b
        ? [200 + 34 * sin(d * 1.5708) * cos(c) * (1 - .12 * d),
           104 + 34 * sin(d * 1.5708) * sin(c) * .5 + 116 * d * d]
        : h
        ? [200 + j * (30 + 52 * max(0, sin(PI * q ** .8)) ** .7) + j * 7 * wv,
           118 + 122 * q + 5 * wv]
        : [200 + (k / 9 - .5) * 46 * (1 - .34 * s)
             + 15 * s ** 1.4 * sin(s * 3.4 - t - 1.5 - k * .42),
           236 + 118 * s ** .92
             + 7 * s * sin(s * 5 - t - 1.2 - k * .3)],
        Dx = 200 + 40 * sin(t * .25 + 4.7) + 16 * sin(t * .5 + 1.3),
        Dy = 200 + 34 * cos(t * .25 + .8) + 12 * sin(t * .75 + 4.7)
       ) => [Dx + (Q[0] - 200) * .74, Dy + (Q[1] - 200) * .74] },

  /* 海 — 頭はほとんど振れない。前から後ろへ波が渡り、後ろほど大きく振れる。
          尾鰭はさらに遅れて、いちばん大きく振る。胸鰭と背鰭は振らずに、体に運ばれるだけ。
          真上からだと尾鰭が線になり、真横からだと体の振れが見えないので、斜めに投影している */
  { slug: '028_same', name: '鮫', ink: 0.155, trail: 0.86, n: 40000, loop: 4,
    f: (i, t,
        b = i < 24000, h = i >= 24000 && i < 31000, g = i >= 31000 && i < 37000,
        s = b ? ((i / 60 | 0) % 400) / 399 : 1,
        c = b ? (i % 60) / 60 * TAU : (i * .754878 % 1) * TAU,
        q = (2 + 58 * s ** 2.4) * sin(s * 2.6 - t * .75),
        W = 33 * max(0, sin(PI * s ** .58)) ** .8,
        e = (i - 24000) / 6999, u = e < .62, a = u ? e / .62 : (e - .62) / .38,
        k = (i * .381966 % 1),
        T = 60 * sin(2.6 - t * .75), L = 60 * sin(2.6 - t * .75 - 1.15),
        j = (i - 31000) % 2 ? 1 : -1, p = ((i - 31000) / 2 | 0) / 2999,
        v = (i * .618034 % 1), P = 6.6 * sin(.78 - t * .75),
        d = (i - 37000) / 2999, z = (i * .754878 % 1), D = .4 + .13 * d,
        G = (2 + 58 * D ** 2.4) * sin(D * 2.6 - t * .75),
        X = b ? q + W * cos(c) : h ? T + (L - T) * k ** .8 + 4 * (u ? 1 : -1) * a * cos(c)
          : g ? P + j * (26 + 68 * max(0, sin(PI * p ** .7)) ** .8) * v ** .5
          : G + 3.4 * (z - .5),
        Z = b ? W * 1.5 * sin(c) : h ? (u ? 100 * a : -58 * a)
          : g ? -8 - 9 * v : 62 * (1 - d ** 1.4) * z ** .8,
        Y = b ? 322 * s : h ? 322 + (9 + 54 * a) * k ** .8
          : g ? 97 + 54 * p + 28 * v : 322 * D + 46 * d,
        Dx = 200 + 27 * sin(t * .25 + 2.2) + 11 * sin(t * .5 + 5.4),
        Dy = 200 + 22 * cos(t * .25 + 4.1) + 9 * sin(t * .75 + 2.2)
       ) => [Dx + (X + .3 * Z) * .74, Dy + (Y - 188 - .5 * Z) * .74] },

  /* 空 — 一羽ずつは進路を決めない。面を渡る二つの波に、少し遅れて従うだけ。
          遅れを個体ごとに変えてあるので束にならない。並びは格子でなく黄金角で散らす
          （整列していると編隊になり、群れにならない）。
          面が横を向くと帯になり、こちらを向くと雲になる。数は変わっていない */
  { slug: '029_mure', name: '群れ', ink: 0.3, trail: 0.9, n: 40000, loop: 4,
    f: (i, t,
        b = i / 32 | 0, k = (i % 32) / 31,
        u = (b * .618034 % 1), v = (b * .754878 % 1),
        d = (b * .381966 % 1) * .9,
        w1 = sin(u * 4.4 - t * .5 + v * 2.2 - d),
        w2 = sin(u * 2.1 + v * 5.3 - t * .5 * 1.5 + 1.1 - d),
        c1 = cos(u * 4.4 - t * .5 + v * 2.2 - d),
        X = (u - .5) * 340 + 26 * w2,
        Y = (v - .5) * 170 + 34 * w1 + 26 * w2 * v,
        Z = 52 * w1 * (.3 + .7 * v) + 20 * sin(u * 6.2 - t * .5 * 2 + v * 3),
        L = 5.4 + 3.2 * (b * .414214 % 1),
        ax = L * (k - .5) * 1.4, ay = L * (k - .5) * c1 * .9,
        r = t * .25 + .5 * sin(t * .5),
        Xr = (X + ax) * cos(r) + Z * sin(r), Zr = Z * cos(r) - (X + ax) * sin(r),
        Dx = 200 + 24 * sin(t * .25 + 3.3) + 10 * sin(t * .5 + 1.7),
        Dy = 200 + 20 * cos(t * .25 + 5.9) + 8 * sin(t * .75 + 3.3)
       ) => [Dx + (Xr + .26 * Zr) * .74, Dy + (Y + ay - .42 * Zr) * .74] },

  /* 空 — ほとんど動かない。体は止まったまま、首だけが回る。
          等速でなく、溜めては素早く回り、また止まる。
          顔の皿は環が寄り集まって音を集める器。こちらを向くと環が見え、横を向くと線に潰れる。
          止まっている部分があるから、回りが生きて見える（第六次の海馬で分かって、使えていなかった） */
  { slug: '030_fukurou', name: '梟', ink: 0.16, trail: 0.9, n: 40000, loop: 4,
    f: (i, t,
        b = i < 21000, h = i < 33000, d = i < 38400,
        s = sin(t * .5), hd = 1.85 * (s < 0 ? -1 : 1) * abs(s) ** .3,
        ch = cos(hd), sh = sin(hd), R = 88,
        u = ((i / 58 | 0) % 362) / 361, a = (i % 58) / 57 * TAU,
        W = 104 * max(0, sin(PI * max(0, min(1, u)) ** .58)) ** .72 + 3.4 * sin(a * 7 + u * 30),
        v = (i - 21000) / 11999 * 2 - 1, r = R * sqrt(max(0, 1 - v * v)), g = i * 2.39996,
        q = (i - 33000) / 5399, k = (i - 33000) % 20,
        dr = k / 19 * 70 * (.4 + .6 * max(0, q) ** .3), da = q * TAU * 9 + k * .3,
        e = (i - 38400) / 1599, er = 15 * sqrt(max(0, e)), ea = e * TAU * 5,
        px = b ? W * cos(a) : h ? r * cos(g) : d ? dr * cos(da) : (i % 2 ? 1 : -1) * 29 + er * cos(ea),
        py = b ? 250 + 232 * (u - .5) : h ? 128 + R * v : d ? 128 + dr * sin(da) * .94 : 118 + er * sin(ea) * .9,
        pz = b ? W * sin(a) * .78 : sqrt(max(0, R * R - px * px - (py - 128) * (py - 128))) * .9,
        X = b ? px : px * ch + pz * sh, Z = b ? pz : pz * ch - px * sh,
        Dx = 200 + 16 * sin(t * .25 + 1.2) + 6 * sin(t * .5 + 4.4),
        Dy = 200 + 12 * cos(t * .25 + 3.8) + 5 * sin(t * .75 + 1.2)
       ) => [Dx + (X + .2 * Z) * .66, Dy + (py - 200 - .3 * Z) * .66] },

  /* 空 — 胴は芯のまわりを巻いている。巻きの位相 S*11 − t/2 が時間で進むので、
          点は体の軸のまわりを回りながら前へ送られ、曲がりの山が尾へ移っていく。
          芯そのものも二つの速さで撓み、巻きの太さは別の波で息をする。
          頭・角・鬚・脚・鬣は、その時刻の胴の枠 (T,N,B) からの局所座標で置く。
          付け根が胴と同じ S を参照するので、動いても部位は離れない */
  { slug: '031_ryuu', name: '龍', grid: false, ink: 0.22, trail: 0.88, n: 40000, loop: 4,
    f: (i, t,
        b = i < 26400, hd = i < 32400, hr = i < 34800, wk = i < 37200, m = (i - 37200) / 700 | 0,
        S = b ? (i / 66 | 0) / 399 : i < 37200 ? 1 : m < 2 ? .8 : .44,
        W3 = S * 11 + t * .5, ro = (9 + 32 * max(0, sin(PI * min(1, S)))) * (1 + .28 * sin(S * 3 + t * .5)),
        rd = 100 * cos(PI * min(1, S)), Cx = 300 * S - 150 + 26 * sin(S * 4 + t * .25),
        Cy = ro * cos(W3) + 46 * sin(S * 3.4 + t * .25), Cz = ro * sin(W3) + 40 * cos(S * 2.6 + t * .5),
        Tx = 300 + 104 * cos(S * 4 + t * .25), Ty = rd * cos(W3) - ro * 11 * sin(W3) + 156 * cos(S * 3.4 + t * .25),
        Tz = rd * sin(W3) + ro * 11 * cos(W3) - 104 * sin(S * 2.6 + t * .5),
        Th = max(.01, hypot(Tx, Tz)), TL = max(.01, hypot(Th, Ty)), G = Th * TL,
        R = 2.2 + 15 * max(0, min(1, S)) ** .45 * (1 - .5 * max(0, min(1, S)) ** 5),
        j = i % 66, a = j / 60 * TAU, cr = j > 59, v = (i * .754878 % 1), z = i % 2 ? 1 : -1,
        e = (i * .618034 % 1) * 2 - 1, g = i * 2.39996, rr = sqrt(max(0, 1 - e * e)),
        p = hr ? ((i - 32400) / 2 | 0) / 1199 : wk ? ((i - 34800) / 2 | 0) / 1199 : (i - 37200) % 700 / 699,
        cm = (3 + 7 * max(0, sin(PI * min(1, S)))) * v, W = S * TAU * 2 + t * .75,
        lt = b ? (cr ? cm * .5 * sin(W + 1) : 0) : hd ? 23 + 30 * e : hr ? 6 - 40 * p - 11 * sin(PI * p)
          : wk ? 46 - 74 * p : -5 - 13 * p + 20 * sin(PI * p) * sin(t * .75 - m * 1.9),
        ln = b ? (cr ? 1.4 * e + cm * .4 * sin(W) : R * cos(a)) : hd ? (12 - 5.5 * e) * rr * cos(g)
          : hr ? z * (5 + 8 * p) : wk ? z * (6 + 12 * p) + 8 * p * sin(p * TAU * 1.25 - t * .75 + z)
          : (m % 2 ? 1 : -1) * (R * .85 + 11 * p),
        lb = b ? (cr ? R + cm : R * sin(a)) : hd ? (10 - 4.5 * e) * rr * sin(g) - 2.4 * max(0, e)
          : hr ? 8 + 30 * p + 9 * sin(PI * p)
          : wk ? -4 - 28 * p + 14 * p * sin(p * TAU * 1.25 - t * .5 + z * 2) : -R * .7 - 44 * p - 10 * sin(PI * p),
        X = Cx + lt * Tx / TL - ln * Tz / Th - lb * Tx * Ty / G, Y = Cy + lt * Ty / TL + lb * Th / TL,
        Z = Cz + lt * Tz / TL + ln * Tx / Th - lb * Ty * Tz / G,
        Dx = 200 + 11 * sin(t * .25 + 2.8), Dy = 200 + 9 * cos(t * .25 + 5.1) + 4 * sin(t * .75 + 2.8)
       ) => [Dx + (X + .28 * Z) * .92, Dy + (Y - .38 * Z) * .92] },

  /* 境 — 地の面を這う。各断片の向きが進行波で、位置はその積分。
          位相を k*S + ω*t と書いてあるので、同じ曲がりは時間とともに小さい S へ、
          つまり頭から尾へ抜けていく。振幅の包み EV は頭で小さく尾で大きいので、
          首と頭は静かなまま、尾ほど大きく振れる。輪は水平の法線と上向きの副法線に
          同じ半径で張ってあり、体の向きによらず丸い管として立つ */
  { slug: '032_hebi', name: '蛇', grid: false, ink: 0.22, trail: 0.9, n: 40000, loop: 4,
    f: (i, t,
        b = i < 33600, hd = i < 39400, S = b ? (i / 70 | 0) / 479 : 1,
        A = S * 12 + t * .5, B2 = S * 4.5 + t * .25, V = S * 7 + t * .5, EV = .3 + .7 * (1 - S),
        P1 = 62 * cos(A) + 40 * cos(B2), P2 = 744 * sin(A) + 180 * sin(B2),
        Tx = 300, Tz = .7 * P1 + EV * P2, Ty = 63 * cos(V),
        Cx = 300 * S - 150, Cz = -EV * P1, Cy = 9 * sin(V),
        Th = max(.01, hypot(Tx, Tz)), TL = max(.01, hypot(Th, Ty)), G = Th * TL,
        R = .5 + 9.2 * max(0, min(1, S)) ** .5 * (1 - .55 * max(0, min(1, S)) ** 8),
        a = (i % 70) / 70 * TAU, e = (i * .618034 % 1) * 2 - 1, g = i * 2.39996,
        rr = sqrt(max(0, 1 - e * e)), z = i % 2 ? 1 : -1, p = (i - 39400) / 599,
        fl = max(0, sin(t * .5)) ** 9, fk = max(0, (p - .55) / .45),
        lt = b ? 0 : hd ? 15 + 23 * e : 36 + 28 * fl * p,
        ln = b ? R * cos(a) : hd ? (16 - 7 * e) * rr * cos(g) : z * 4.4 * fk * fl,
        lb = b ? R * sin(a) : hd ? (8.6 - 3.7 * e) * rr * sin(g) + 1.6 : 1.8 - 2.4 * p * fl,
        X = Cx + lt * Tx / TL - ln * Tz / Th - lb * Tx * Ty / G, Y = Cy + lt * Ty / TL + lb * Th / TL,
        Z = Cz + lt * Tz / TL + ln * Tx / Th - lb * Ty * Tz / G,
        Dx = 200 + 12 * sin(t * .25 + 1.9), Dy = 200 + 9 * cos(t * .25 + 4.1) + 4 * sin(t * .75 + 2)
       ) => [Dx + (X + .18 * Y) * .86, Dy + (Z * .84 - .5 * Y) * .86] },

  /* 象り — 顔を描いていない。同じ四万点が、形のない雲と猫の顔のあいだを行き来する。
          澄み cl が20秒に三度だけ尖り、そのあいだ位置は雲から顔へ寄る。
          目と口だけ澄みを二乗してあるので、**表情は輪郭より先に消え、あとから入る**。
          点は一つも増えず一つも減らない。顔は点どうしの関係にしか無い */
  { slug: '033_kao', name: '顔', grid: false, ink: 0.17, trail: 0.9, n: 40000, loop: 4,
    f: (i, t,
        b = i < 18000, ey = i < 26000, mu = i < 31600, wh = i < 37000, er = i < 39000,
        E1 = sin(t * .25), E2 = sin(t * .5 + 1.7), bl = max(0, sin(t * .75 + .4)) ** 16,
        eo = (.72 + .28 * E2) * (1 - bl), mc = 11 * E1 + 5 * E2, ea = .1 * E1, wq = 8 * E2,
        mq = 21 * max(0, sin(t * .5 + 2.4)) ** 3, br = 1 + .022 * sin(t * .5),
        cl = (.5 + .5 * cos(t * .75 + .8)) ** 1.3, cg = i > 17999 && mu ? cl * cl : cl,
        u = i * .618034 % 1, g = i * 2.39996, v = i * .754878 % 1, nn = i * .324717 % 1,
        w = sqrt((i - 18000) % 4000 / 3999), s = w * cos(g), h = w * sin(g), k = i < 22000 ? -1 : 1,
        A = i / 18000 * TAU - PI, q = (i - 26000) % 2800 / 2799 * 2 - 1,
        R = 62 * max(0, 1 - abs(A + 2.42 + ea) / .4) + 62 * max(0, 1 - abs(A + .72 - ea) / .4),
        j = (i - 31600) / 900 | 0, p = (i - 31600) % 900 / 899, d = (i - 37000) % 1000 / 999,
        c = i < 38000 ? -2.42 - ea : -.72 + ea, cr = 176 * max(0, v) ** .9,
        ct = nn * TAU + t * .25 + 1.4 * sin(v * 6 + t * .5), gx = cr * cos(ct), gy = cr * sin(ct) * .92,
        X = b ? (132 + R) * br * cos(A) : ey ? k * 55 + 19 * s : mu ? 31 * q
          : wh ? (j < 3 ? -1 : 1) * (66 + 46 * p) : er ? (142 + 38 * d) * cos(c)
          : 7 * sqrt((i - 39000) / 999) * cos(g),
        Y = b ? (117 + R) * br * sin(A)
          : ey ? 15 + eo * 19 * h + (1 - eo) * (3 * h - 12 * (1 - s * s))
          : mu ? 56 + (mc + (i < 28800 ? 0 : mq)) * (1 - q * q)
          : wh ? 26 + j % 3 * 18 + p * (j % 3 - 1) * 10 + p * wq
          : er ? (127 + 38 * d) * sin(c) : 39 + 7 * sqrt((i - 39000) / 999) * sin(g),
        Dx = 200 + 9 * sin(t * .25 + 1.1), Dy = 196 + 7 * cos(t * .25 + 3.4) + 3 * sin(t * .75 + 1.1)
       ) => [Dx + gx + cg * (X - gx), Dy + gy + cg * (Y - gy)] },

  /* 海 — 二千匹の小魚が球になっている。描いていないものが一つ通る。
          点 P が二十秒でひと巡りする道を進み、近い魚ほど外へ避けるので、
          群れの中を空洞が移っていく。捕食者はどの点にも描かれていない。
          見えるのは、他の魚の避けかた＝関係の模様だけである */
  { slug: '034_iwashidama', name: '鰯玉', grid: false, ink: 0.24, trail: 0.88, n: 40000, loop: 4,
    f: (i, t,
        j = i / 20 | 0, k = (i % 20) / 19,
        u = j * .618034 % 1, v = j * .754878 % 1, w2 = j * .414214 % 1,
        y0 = 2 * u - 1, rr = sqrt(max(0, 1 - y0 * y0)), sp = j % 2 ? .5 : .25,
        A = v * TAU + t * sp + .3 * sin(y0 * 4 + t * .5),
        R = 104 + 9 * sin(t * .5 + w2 * TAU),
        x0 = R * rr * cos(A), z0 = R * rr * sin(A), Y0 = R * y0 * .92,
        Px = 96 * cos(t * .25 + 2.1), Py = 44 * sin(t * .5), Pz = 96 * sin(t * .25 + 2.1),
        dx = x0 - Px, dy = Y0 - Py, dz = z0 - Pz, q = dx * dx + dy * dy + dz * dz,
        s = sqrt(q), M = 92 * exp(0 - q / 3600) / (s + 4) * (1 + .5 * k),
        tx = 0 - sin(A), tz = cos(A), L = 8 + 3 * w2,
        X = x0 + dx * M + tx * L * (k - .5), Z = z0 + dz * M + tz * L * (k - .5),
        Y = Y0 + dy * M + 1.2 * (i * .324717 % 1 - .5),
        Dx = 200 + 14 * sin(t * .25 + .7) + 6 * sin(t * .5 + 3.1),
        Dy = 200 + 11 * cos(t * .25 + 2.9) + 5 * sin(t * .75 + .7)
       ) => [Dx + (X + .3 * Z) * .72, Dy + (Y - .35 * Z) * .72] }

];
